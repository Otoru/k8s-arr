---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: torrents.torrents.vitoru.fun
spec:
  group: torrents.vitoru.fun
  names:
    kind: Torrent
    listKind: TorrentList
    plural: torrents
    singular: torrent
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Torrent is the Schema for the torrents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TorrentSpec defines the desired state of Torrent
            properties:
              indexer:
                description: Indexer that provided this torrent
                type: string
              infoHash:
                description: InfoHash of the torrent (optional, can be extracted from
                  magnet)
                type: string
              leechers:
                description: Leechers count at time of discovery
                type: integer
              magnet:
                description: Magnet link
                type: string
              publishedAt:
                description: PublishedAt is when the torrent was uploaded
                format: date-time
                type: string
              seeders:
                description: Seeders count at time of discovery
                type: integer
              size:
                description: Size of the content (string representation, e.g., "1.5
                  GB")
                type: string
              title:
                description: Title of the torrent release
                type: string
            required:
            - magnet
            - title
            type: object
          status:
            description: TorrentStatus defines the observed state of Torrent
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
